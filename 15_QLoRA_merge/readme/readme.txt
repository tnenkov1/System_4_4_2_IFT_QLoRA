Python script for merging LoRA adapters from fine-tuning with the base model

1. Move the LoRA adapters into folder: "LoRA"
2. Move the base model into folder: "LLM"
3. Execute the script in Windows cmd
4. The merged fine-tuned model will be in the folder: "FineTunedModel"